# 2026-03-04 수정 내역

## 1) 인증/권한 안정화
- Firebase 로그인 후 Supabase 프로필 검증 흐름을 점검하고, 프로필/권한 누락 시 처리 로직을 정리했습니다.
- 관리자 계정(`cryingonion77@gmail.com`)을 `is_admin=true`, `is_approved=true`로 반영했습니다.

## 2) Supabase 초기화 정리
- 신규 Supabase 프로젝트에서 발생한 `PGRST205`(테이블 없음) 이슈 원인을 확인했습니다.
- 프로젝트에서 사용하는 주요 테이블과 기본 정책을 한 번에 생성할 수 있는 초기 스키마를 준비했습니다.

## 3) 문서 정리
- 기존 `docs` 하위의 기존 `.md` 문서를 모두 정리했습니다.
- 본 문서와 서비스 요약 문서 2개만 유지하도록 구조를 단순화했습니다.
